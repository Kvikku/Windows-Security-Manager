<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WindowsSecurityManager.Gui.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:WindowsSecurityManager.Gui.ViewModels">

    <ScrollViewer Padding="32">
        <StackPanel Spacing="24">
            <!-- Header -->
            <StackPanel Orientation="Horizontal" Spacing="12">
                <FontIcon Glyph="&#xE83D;" FontSize="32" Foreground="{ThemeResource AccentFillColorDefaultBrush}" />
                <TextBlock Text="Security Dashboard" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center" />
            </StackPanel>

            <!-- Overall compliance card -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="24" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                <Grid ColumnDefinitions="*,Auto">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Overall Compliance" Style="{StaticResource SubtitleTextBlockStyle}" />
                        <StackPanel Orientation="Horizontal" Spacing="16">
                            <TextBlock x:Name="ComplianceText" Style="{StaticResource HeaderTextBlockStyle}" />
                        </StackPanel>
                        <ProgressBar x:Name="ComplianceBar" Maximum="100" Height="8" CornerRadius="4" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Summary cards -->
            <Grid ColumnDefinitions="*,*,*" ColumnSpacing="16">
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8" Padding="20" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <StackPanel Spacing="4">
                        <TextBlock Text="Hardened" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock x:Name="EnabledText" Style="{StaticResource SubtitleTextBlockStyle}" Foreground="Green" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8" Padding="20" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <StackPanel Spacing="4">
                        <TextBlock Text="Misconfigured" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock x:Name="DisabledText" Style="{StaticResource SubtitleTextBlockStyle}" Foreground="Orange" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8" Padding="20" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <StackPanel Spacing="4">
                        <TextBlock Text="Missing" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock x:Name="NotConfiguredText" Style="{StaticResource SubtitleTextBlockStyle}" Foreground="Red" />
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Category breakdown -->
            <TextBlock Text="Category Breakdown" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,8,0,0" />
            <ItemsRepeater x:Name="CategoryRepeater">
                <ItemsRepeater.Layout>
                    <StackLayout Spacing="12" />
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="vm:CategoryComplianceItem">
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                CornerRadius="8" Padding="16" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                            <Grid RowDefinitions="Auto,Auto" ColumnDefinitions="*,Auto">
                                <TextBlock Text="{x:Bind DisplayName}" FontWeight="SemiBold" />
                                <TextBlock Grid.Column="1" FontWeight="SemiBold">
                                    <Run Text="{x:Bind EnabledCount}" /><Run Text=" / " /><Run Text="{x:Bind TotalCount}" />
                                </TextBlock>
                                <ProgressBar Grid.Row="1" Grid.ColumnSpan="2"
                                             Value="{x:Bind CompliancePercentage}" Maximum="100"
                                             Height="6" CornerRadius="3" Margin="0,8,0,0" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>

            <!-- Refresh button -->
            <Button Content="Refresh" Click="Refresh_Click" HorizontalAlignment="Left" Style="{StaticResource AccentButtonStyle}" />
        </StackPanel>
    </ScrollViewer>
</Page>
